--- a/talloc.pc.in
+++ b/talloc.pc.in
@@ -6,6 +6,6 @@
 Name: talloc 
 Description: A hierarchical pool based memory system with destructors
 Version: @TALLOC_VERSION@
-Libs: @LIB_RPATH@ -L${libdir} -ltalloc
+Libs: @LIB_RPATH@ -L${libdir} -L/lib -ltalloc -lbsd
 Cflags: -I${includedir} 
 URL: http://talloc.samba.org/
